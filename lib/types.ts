export type EventType = "conference" | "retreat" | "prayer" | "outreach" | "giveaway" | "upper-room";

export type AgentSuggestion = {
  title: string;
  description: string;
  cta: string;
  href: string;
};
